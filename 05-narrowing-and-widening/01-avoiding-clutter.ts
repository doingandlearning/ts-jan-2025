// function definitions
// use function expressions - we can type before we get there.

import type { Person } from "./types";

const person: Person = {};

person.name = "Dan";
person.born = { where: "Manchester", when: 1999 };
