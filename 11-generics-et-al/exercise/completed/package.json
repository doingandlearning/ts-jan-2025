{
  "name": "04-generics-exercise-completed",
  "scripts": {
    "preserve": "rm -rf dist && mkdir dist && cp static/* dist/",
    "serve": "npx esbuild src/app.ts --bundle --format=esm --sourcemap --outdir=dist/js --servedir=dist"
  },
  "dependencies": {
    "@total-typescript/tsconfig": "^1.0.4"
  }
}
